[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[all,standard]>=0.135.2",
    "uvicorn[standard]",
    "psycopg2-binary",
    "python-dotenv",
    "boto3",
    "sqlalchemy",
]
